var comments = document.getElementById('comments_count');
var count = parseInt(comments.innerHTML) + 1;
comments.innerHTML = count;

var comment_list = document.getElementById('comment_list');
content = comment_list.innerHTML + "<%= j render 'comments/comment', comment: @comment %>";
comment_list.innerHTML = content;

document.getElementById('comment_content').value = '';